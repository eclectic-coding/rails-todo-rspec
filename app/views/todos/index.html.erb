
<ul class="todos">
  <% @todos.each do |todo| %>
  <li><%= todo.title %></li>
  <% end %>
</ul>

<%= link_to 'Add a new Todo', new_todo_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>